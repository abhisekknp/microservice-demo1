spring:
  application:
    name: webservice-registry
  cloud:
    config:
       uri: http://10.136.99.20:8888
      
---

spring:
  profiles: docker
  cloud:
     config:
        uri: http://configserver:8888
